venv/
__pycache__/
*.pyc
*.pyo
.DS_Store

# Service account key files — never commit these
credentials/*.json

# Auth — never commit these
users.json
secret.key

# Cloudflare local secrets / generated files
.dev.vars
.wrangler/
worker-configuration.d.ts
node_modules/
